// @flow

'use strict';

/**
 * @param {number[]} nums
 * @param {number} target
 * @return {[number, number]}
 */
export default function twoSum(nums: number[], target: number): [number, number] {
    return false;
}